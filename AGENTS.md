# Sagent 项目全局开发约束

本文件适用于 `sagent` 仓库中的所有 crate、二进制程序、测试和文档。子目录如果新增更具体的 `AGENTS.md`，可以补充本文件，但不得降低这里的要求。

围绕 StorageManager、业务 Storage、单一职责和分层实现的完整代码规范见
[`docs/design/rust-code-standards.md`](docs/design/rust-code-standards.md)；本文件中的强制
约束优先级更高，详细规范用于解释和落实这些约束。

## 代码注释要求（强制）

- 新增或修改生产代码时，必须同步补充准确的中文注释或 Rust 文档注释；不能只提交代码而省略说明。
- 注释优先说明 **为什么这样设计**、不变量、边界条件、副作用和生命周期，不要机械翻译代码或重复变量名、函数名。
- 以下关键逻辑必须有注释：
  - 会话、消息、Turn、Generation、Command 和 Event 的状态转换及其合法顺序；
  - Actor、mailbox、异步任务、并发访问和 `CancellationToken` 的协作关系；
  - 数据库事务、写入顺序、幂等性、回滚、迁移和索引/FTS 查询规则；
  - JSON-RPC 方法分发、参数校验、序列化格式和错误映射；
  - Provider 流式响应、SSE 分行解析、结束事件、用量统计和重试边界；
  - 工具调用、审批选项、权限/安全检查、进程生命周期和敏感信息处理；
  - Profile 隔离、路径解析、配置覆盖以及跨 crate 的公共协议约束。
- 新增或修改公共的 `struct`、`enum`、`trait`、常量、函数、字段和枚举变体时，必须使用 `///` 说明用途、生命周期、取值范围或错误语义；字段注释应说明持久化含义和单位（如适用）。
- 修改函数逻辑时，在函数级注释和关键分支附近说明“输入 → 决策 → 输出/失败处理”；涉及并发或安全边界时，必须解释采取该限制的原因。
- 测试代码也要通过测试名、fixture 注释或 Arrange/Act/Assert 注释说明被验证的行为契约；不要只用注释复述显而易见的断言。
- 注释必须与实现保持同步，不得保留过时的阶段性说明；不得在注释、日志或测试 fixture 中写入真实 API key、令牌或其他秘密。
- 代码评审前应检查 diff 中新增的非显然逻辑是否有必要注释。格式化和验证至少运行：
  `cargo fmt --all`、`cargo test --workspace`、`cargo clippy --workspace --all-targets -- -D warnings` 和 `git diff --check`。

## Rust 工程约束

- **单一职责优先（强制）**：每个 crate、模块、`struct`、`enum`、服务对象和函数必须有一个能用一句话描述的主要职责，并只因同一类业务变化而修改。发现一个对象同时承担状态管理、业务决策、持久化、网络/进程 I/O、协议转换或 UI 呈现中的多项职责时，必须按边界拆分；不得以“方便调用”为理由继续堆入同一对象。
- **数据、编排与副作用分离（强制）**：领域类型和协议 DTO 负责表达数据与不变量；Store/Repository 负责持久化与事务；Service/Actor 负责状态推进和编排；Transport/CLI/TUI 负责输入输出适配；工具执行器负责具体 I/O。入口层不得直接堆积业务规则，领域对象不得直接打开数据库、读取配置、发网络请求或操作终端，除非该对象的唯一职责正是该项基础设施适配。
- **依赖方向清晰**：低层 crate 不得依赖上层入口、UI 或具体运行时实现。配置层只解析和验证配置，不负责创建 HTTP 客户端、Provider 或后台任务；协议层保持可被客户端复用的 DTO、错误语义和接口，不混入 Store/CLI 的具体业务实现。新增依赖前必须说明其所属层级和必要性。
- **避免伪抽象**：仅在已有至少一个明确调用方、可验证的变化点或已知扩展需求时引入 trait、注册表、泛型框架或新 crate。不能用“未来可能需要”创建空扩展点；优先提取有真实职责的私有函数或模块。
- **文件规模**：生产 Rust 文件目标为 200–450 行；超过 500 行时必须在评审中说明为何无需拆分并给出按职责切分的判断；超过 700 行必须拆分为同主题子模块或 facade + siblings。测试文件目标不超过 600 行；超过时应优先抽共享 fixture、harness 或按行为场景拆分。行数是触发设计审查的信号，不得为满足数字机械移动代码。
- **函数规模与复杂度**：普通函数目标不超过 60 行；状态机、事务和 I/O 编排函数目标不超过 100 行；超过 120 行必须拆分为具名的决策、准备、执行或收尾步骤。事务原子性、Actor 串行状态转换和资源清理必须保持在同一明确边界内，不能为缩短函数而破坏一致性、取消语义或错误处理。
- **模块组织**：一个模块只处理一个主题；对外入口保持薄，只负责组合和暴露稳定 API。新增行为应进入所属主题模块，禁止持续向 `lib.rs`、`main.rs`、单一 service 或单一 actor 追加无关职责。拆分后不得通过循环依赖、全局可变状态或无意义 re-export 掩盖边界问题。
- **异步任务所有权**：每个 `tokio::spawn` 必须有明确的所有者、取消来源、等待或 abort 策略以及资源释放路径。长生命周期任务应由对应 supervisor/actor 管理，不能隐藏在工具、DTO 或入口辅助函数中。
- 保持 crate 职责清晰：协议类型、持久化、核心服务、CLI/RPC 入口分别放在对应 crate，不在入口文件堆积业务逻辑。
- 优先复用已有类型和错误定义，避免重复建模；跨 crate 的公共类型变更必须同步检查序列化、数据库和调用方。
- 错误必须保留上下文并映射到稳定的公共错误语义；禁止用 `unwrap`/`expect` 掩盖可恢复的运行时错误。
- 异步代码应明确取消、超时和资源释放行为；不得引入无法停止的后台任务或无界队列。
- 数据库写操作应明确事务边界和失败后的可见状态；查询应保持与项目协议、数据模型和设计文档一致。

## 架构边界与可替换性（强制）

- **统一持久化边界**：所有生产业务读写必须经由领域存储接口或由其创建的 repository；Runtime、RPC、CLI、工具和 TUI 不得依赖 `rusqlite::Connection`、SQL 字符串或具体数据库路径。允许 SQLite 实现内部直接使用 `rusqlite`。新接口按 Session/Turn、查询、搜索、调度等领域拆分，禁止把全部读写方法堆进万能 `Storage` trait。
- **事务是领域契约**：跨消息、Generation、工具结果和事件的一次原子状态变化必须以一个高层存储操作表达，例如 `commit_tool_result` 或 `complete_turn`。不得把同一事务拆成多个上层写调用；每个后端实现都必须提供相同的原子性、幂等性、可见性和错误语义。
- **后端选择与密钥边界**：配置只解析 `StorageDescriptor`、`ProviderDescriptor`、workspace 和 policy 等不可变描述符。Factory/Bootstrap 才创建 SQLite、远程存储、Provider 或网络客户端；密钥只在创建实现时读取，不能进入公开 DTO、日志、RPC 或可序列化配置。
- **按变化点抽象**：需要替换实现的边界使用小型 trait/接口与 factory，例如 Storage、Provider、工具执行器、transport framing；纯数据类型、单一算法、单个工具内部细节和尚无第二实现的代码不得为了“统一”提前 trait 化。每新增抽象必须写明已有调用方、替换目标和行为契约。
- **Provider 与能力解析**：ProviderFactory 负责将 descriptor 变成 `ModelProvider`；ProviderRouter/CapabilityResolver 负责按 Profile、Session、Turn 决策。每个 Turn 开始时生成不可变 `GenerationResolution`（provider/model、tool schema、prompt、policy revision 等）；运行中不得修改其能力集合，也不得让 Supervisor 保存可被回合间隐式共享的可变能力状态。
- **工具双边界**：ToolCatalog/Registry 只负责工具定义、schema、版本和风险元数据；ToolExecutor 只负责执行已验证调用。审批、workspace、取消令牌、会话身份等通过显式 `ToolExecutionContext` 传递。新增工具必须同时可被列出、验证和执行，不能只注册 schema 或只添加 Worker 分支。
- **协议与传输分层**：Protocol crate 只包含 DTO、稳定错误语义、方法定义和 service trait；具体 Store/Runtime 服务实现放在 RPC/Service 层。Transport 复用统一的连接生命周期（入站、dispatch、出站、取消、任务收口），stdio/WebSocket 等实现只处理 framing，不能各自复制业务分发或关闭逻辑。
- **配置状态必须显式**：不要用多个无关联 `Option<T>` 表达互斥或依赖的运行模式。使用 enum、已验证的配置结构或 builder 在 bootstrap 时消除非法组合；运行时对象只接收完整、可用的依赖包。

## 可读性与维护性（强制）

- **流程代码的阅读顺序**：包含校验、决策、持久化、异步执行和事件发布的函数必须按“输入校验 → 决策/计划 → 原子持久化 → 执行/等待 → 状态更新/发布”组织，并提取具名步骤。函数名使用业务意图，如 `persist_tool_call_plan`，不得以模糊的 `process`、`handle`、`run` 掩盖多重职责。
- **模块地图**：每个 crate 根、公共模块和超过 300 行的生产模块必须有模块级 Rustdoc，写明负责什么、不负责什么、关键不变量以及与相邻模块的协作边界。对外 facade 只暴露稳定入口；实现细节进入按主题命名的 sibling/module。
- **稳定的错误层次**：跨 crate 或 RPC 边界使用领域错误类型（Storage、Provider、Tool、Runtime、Protocol 等）表达稳定失败类别；内部错误可附带 `anyhow` 上下文。不得把 SQLite/HTTP/serde 等实现错误类型或敏感 endpoint 直接泄漏到公开响应。
- **语义化配置值**：容量、超时、重试次数、版本/revision 和限制值必须拥有领域名称，并集中在所属配置/策略对象或常量中；不得在多个函数散落相同数字或字符串。修改默认值时必须说明影响范围并有边界测试。
- **测试可读性**：测试正文只保留行为契约；重复的临时目录、Store、Provider、RPC/TUI 启动过程应抽为小型、透明且按领域命名的 fixture/harness。fixture 不得成为隐式业务实现或万能测试框架；测试必须能说明验证的状态变化、事件顺序或安全边界。
- **重构提交纪律**：结构整理、行为变更、格式化/文档和 fixture 重组尽量分开提交。重构必须先以 contract、集成或端到端测试锁定外部行为；禁止借重构偷偷改变协议、持久化语义、权限边界或默认配置。

## 变更检查清单

1. 是否为新增/修改的关键逻辑补充了中文注释，并解释设计原因？
2. 是否能用一句话说明每个新增/修改模块、对象和函数的唯一职责？是否把数据、编排和 I/O 分开？
3. 文件或函数是否达到规模审查阈值；若达到，是否完成按职责拆分或记录了不拆分的原子性理由？
4. 此处是否处于真实可替换边界？若是，是否使用小型领域接口、Factory 和行为契约，而非万能对象或伪抽象？
5. 是否避免让配置创建基础设施、让协议绑定存储、让入口层堆积业务，或让回合中途改变 capability？
6. 是否同步更新了受影响的公共类型、协议、数据库和测试？
7. 是否运行格式化、工作区测试、Clippy 和 diff 检查？
8. 是否确认没有提交密钥、令牌、机器相关路径或无关生成文件？
